import styled from 'styled-components';

import Toolbar from '../Toolbar';

export default styled(Toolbar)`
  margin-top: 80px;
  margin-bottom: 50px;
  .pageToolbar-backButton {
    position: absolute;
    top: -50px;
    left: 0;
  }
`;
