import styled from 'styled-components';

const Toolbar = styled.div`
  display: flex;
  position: relative;
  align-items: center;
  padding-left: 20px;
  padding-right: 20px;
  min-height: 72px;
`;

export default Toolbar;
